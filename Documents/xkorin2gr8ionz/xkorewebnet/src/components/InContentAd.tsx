// MainContainer.tsx
import React from 'react';
import PropTypes from 'prop-types'    

const InContentAd: React.FC = () => {
  return (
    <div></div>
  );
};

InContentAd.propTypes = {
    position: PropTypes.string.isRequired,
  };

export default InContentAd;
